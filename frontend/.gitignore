# Dependencies (never upload these - too big!)
/node_modules      

# Next.js build files (auto-generated)
/.next/           

# Environment variables (my secrets!)
.env*             
.env*.local       
.env.local